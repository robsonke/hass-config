# Bubble Card native pop-up (hash #verlichting-tuin) — Verlichting light sliders.
# Light slider cards (drag = brightness, tap = toggle, colour = light's own rgb);
# on/off-only lights are plain toggle cards. See the popup-modernization spec.
type: custom:bubble-card
card_type: pop-up
buttons_position: left
popup_mode: centered
full_width_on_mobile: true
width_desktop: 1000px
hash: '#verlichting-tuin'
# sluit automatisch na 1 min zonder interactie; elke tap/klik reset de timer
auto_close: 60000
name: Tuin verlichting
icon: mdi:lightbulb-group
entity: light.lights_garden
# Alle vier de regels hieronder lazen dezelfde 19-entity lijst uit een hardcoded
# array - vier kopieen die uit de pas liepen met de light group in
# packages/pkg_lights.yaml (de lantaarns stonden alleen hier in, waardoor de
# tile op het dashboard "1 aan" meldde terwijl er vier lampen brandden).
# Nu komt de ledenlijst uit de group zelf, net als bij de tile: een plek om te
# onderhouden.
state_content: >-
  {%- set m = expand('light.lights_garden') -%}
  {%- set o = m | selectattr('state','eq','on') | list | count -%}
  {{ 'Alles uit' if o == 0 else o ~ ' aan · ' ~ (m | count - o) ~ ' uit' }}
styles: |
  .bubble-button-background { opacity: 1 !important; background-color: ${(hass.states['light.lights_garden']?.attributes?.entity_id || []).some(e => hass.states[e]?.state === 'on') ? 'rgba(240,195,95,0.13)' : 'rgba(255,255,255,0.04)'} !important; }
  .bubble-icon-container { background-color: ${(hass.states['light.lights_garden']?.attributes?.entity_id || []).some(e => hass.states[e]?.state === 'on') ? 'rgba(240,195,95,0.20)' : 'rgba(255,255,255,0.07)'} !important; }
  .bubble-state { font-size: 14px !important; font-weight: 500; color: ${(hass.states['light.lights_garden']?.attributes?.entity_id || []).some(e => hass.states[e]?.state === 'on') ? '#f0cf8f' : '#9b9aac'} !important; }
cards:
  - type: custom:bubble-card
    card_type: separator
    name: Overkapping
  - type: grid
    columns: 2
    square: false
    cards:
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.roofceiling_garden_light
        name: Spots
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.roofceiling_table_light
        name: Tafel
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.lights_wall_shed
        name: Wand
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.veranda_light
        name: Blokhut Spots
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: state
        entity: light.shed_light_back
        name: Schuur Achter
        card_layout: large
        tap_action: { action: toggle }
        hold_action: { action: more-info }
        state_content: "{{ 'aan' if is_state('light.shed_light_back','on') else 'uit' }}"
        styles: |
          .bubble-button-background { opacity: 1 !important; background-color: ${state === 'on' ? 'rgba(240,195,95,0.14)' : 'rgba(255,255,255,0.04)'} !important; }
          .bubble-icon-container { background-color: ${state === 'on' ? 'rgba(240,195,95,0.20)' : 'rgba(255,255,255,0.07)'} !important; }
          .bubble-state { font-size: 12.5px !important; color: ${state === 'on' ? '#f0cf8f' : '#8f8e9e'} !important; }
  - type: custom:bubble-card
    card_type: separator
    name: Tuin
  - type: grid
    columns: 2
    square: false
    cards:
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.garden_light
        name: Wandlampen
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.woodshed_light
        name: Houthok
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.pool_terrace_light1
        name: Zwembad hoek 1
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.pool_terrace_light2
        name: Zwembad hoek 2
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.palm1_light
        name: Palm 1
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.palm2_light
        name: Palm 2
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
  - type: custom:bubble-card
    card_type: separator
    name: Sfeer
  - type: grid
    columns: 2
    square: false
    cards:
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.garden_lantern_light1
        name: Lantaarn 1
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.garden_lantern_light2
        name: Lantaarn 2
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.garden_lantern_light3
        name: Lantaarn 3
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.hue_lily_light_fence_1
        name: Hek 1
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.hue_lily_light_fence_2
        name: Hek 2
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.hue_lily_light_fence_3
        name: Hek 3
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.hue_lily_light_tree_1
        name: Boom Eva
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
      - type: custom:bubble-card
        card_type: button
        button_type: slider
        entity: light.hue_lily_light_tree_2
        name: Boom Noa
        card_layout: large
        slider_live_update: true
        tap_action: { action: toggle }
        double_tap_action: { action: more-info }
        hold_action: { action: more-info }
        sub_button:
          - icon: mdi:palette
            tap_action: { action: more-info }
            show_background: true
  # ── tijdlijn ──────────────────────────────────────────────────────
  # "Automatisch aan" is the input_boolean the garden darkness automation sets
  # itself, cleared by the morning off automation. Note it deliberately stays on
  # through the night: the evening automation only dims the lanterns back to
  # 30%, it does not end the dark stretch. The lux line underneath is the sensor
  # that automation triggers on (< 12).
  # Reaches back at most recorder's purge_keep_days (10).
  - type: custom:bubble-card
    card_type: separator
    name: Tijdlijn
  - type: history-graph
    title: Automatisch aan door donker (4 dagen)
    hours_to_show: 96
    entities:
      - entity: input_boolean.lights_garden_auto_dark
        name: Automatisch aan
      - entity: light.lights_garden
        name: Alle lampen tuin

#  ____
# |  _ \ ___ _ __ ___  ___  _ __  ___
# | |_) / _ \ '__/ __|/ _ \| '_ \/ __|
# |  __/  __/ |  \__ \ (_) | | | \__ \
# |_|   \___|_|  |___/\___/|_| |_|___/
#
# Author: Rob Sonke
# https://github.com/robsonke/hass-config
#

person:
  - name: Rob
    id: rob
    user_id: 56ef6157f087451399aa545f32dc78d3
    device_trackers:
      #- device_tracker.rob_presence
      - device_tracker.iphone_rob
      - device_tracker.iphonedetect_rob
      - device_tracker.apple_watch_rob
  - name: Steffi
    id: steffi
    user_id: 71e44d7d313b4695a8bf51705f7adfc3
    device_trackers:
      #- device_tracker.steffi_presence
      - device_tracker.iphone_steffi
      - device_tracker.iphonedetect_steffi

notify:
  - platform: group
    name: all_phones
    services:
      - service: mobile_app_iphone_rob
      # wrong name, but oke
      - service: mobile_app_iphone
      #- service: mobile_app_iphone_steffi

homeassistant:
  customize_domain:
    light:
      assumed_state: false
  customize:
    person.rob:
      entity_picture: "/local/images/rob_icon-mirror.png"
    person.steffi:
      entity_picture: "/local/images/steffi_icon.png"

sensor:
  - platform: history_stats
    name: Cat door today
    entity_id: binary_sensor.motion_sensor_cats_occupancy
    state: "on"
    type: count
    start: "{{ now().replace(hour=0, minute=0, second=0) }}"
    end: "{{ now() }}"
  - platform: history_stats
    name: Bijkeuken cat sightings today
    entity_id: binary_sensor.bijkeuken_cat_occupancy
    state: "on"
    type: count
    start: "{{ now().replace(hour=0, minute=0, second=0) }}"
    end: "{{ now() }}"

template:
  - binary_sensor:
    - name: "Noa Bed Presence"
      unique_id: noa_bed_presence
      device_class: occupancy
      availability: >
        {{ has_value('binary_sensor.noabed_person_occupancy') and has_value('binary_sensor.noabed_all_occupancy') }}
      state: >
        {{ is_state('binary_sensor.noabed_person_occupancy', 'on') or is_state('binary_sensor.noabed_all_occupancy', 'on') }}
      delay_off:
        minutes: 15
    - name: "Eva Bed Presence"
      unique_id: eva_bed_presence
      device_class: occupancy
      availability: >
        {{ has_value('binary_sensor.evabed_person_occupancy') and has_value('binary_sensor.evabed_all_occupancy') }}
      state: >
        {{ is_state('binary_sensor.evabed_person_occupancy', 'on') or is_state('binary_sensor.evabed_all_occupancy', 'on') }}
      delay_off:
        minutes: 15
default:
  color_list: ['red', 'green', 'blue']

energy_chart:
  graph_span: 24h
  config_templates: default
  span:
    start: day
  now:
    show: true
  header:
    show: true
    show_states: true
    colorize_states: true
  all_series_config:
    stroke_width: 1
    opacity: 0.5
    type: area